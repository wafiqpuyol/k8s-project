apiVersion: v1
kind: Service
metadata: 
  name: project2-backend-service
  namespace: project2
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      nodePort: 30007